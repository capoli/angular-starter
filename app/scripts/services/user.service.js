(function () {
    'use strict';

    angular.module('app.services')
        .factory('UserService', ['$q', '$http', 'ConfigService',
            function ($q, $http, ConfigService) {
                
        }]);
})();